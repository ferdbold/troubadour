{
  "name": "troubadour",
  "version": "0.1.0",
  "description": "Control your music from the cloud and smart lights with MIDI-enabled devices",
  "main": "index.js",
  "repository": "git@github.com:ferdbold/troubadour.git",
  "author": "Frédéric Bolduc <me@ferdbold.com>",
  "license": "GPL-3.0-or-later",
  "devDependencies": {
    "concurrently": "^5.1.0",
    "dotenv": "^8.2.0"
  },
  "scripts": {
    "start": "yarn run server",
    "dev-start": "concurrently \"yarn run server\" \"yarn --cwd ./client start\" \"mongo\"",
    "server": "node index.js",
    "heroku-postbuild": "cd client && yarn && yarn run build"
  },
  "dependencies": {
    "body-parser": "^1.19.0",
    "cookie-parser": "^1.4.4",
    "cors": "^2.8.5",
    "express": "^4.17.1",
    "mongodb": "^3.5.4",
    "querystring": "^0.2.0",
    "request": "^2.88.2"
  }
}
